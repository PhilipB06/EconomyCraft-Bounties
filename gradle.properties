org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Addon properties
mod_version=1.0.0
maven_group=com.reazip.economycraft.bounties
archives_name=economycraft-bounties
economycraft_version=1.10.0

# Default target (overridden by the per-version tasks in build.gradle)
minecraft_version=26.3
fabric_loader_version=0.19.5
# fabric_api_version / neoforge_version are intentionally NOT set here

loom.allowMismatchedPlatformVersion=true
